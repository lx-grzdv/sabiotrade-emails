# breach-winback

Текст: `breach_winback_1.docx`

_потерял аккаунт, ушёл_

Статус: HTML готов, кампания вынесена в `preview/breach-winback/`.

Когда HTML готов — скопировать в `preview/{slug}/` и обновить `index.html`.
